import { StoryObj } from '@storybook/react';

declare const meta: {
    title: string;
    parameters: {
        layout: string;
    };
};
export default meta;
type Story = StoryObj;
export declare const Default: Story;
